# GraphCombEx (binary release)

This project contains binary distributable files for `GraphCombEx` for Windows.
There are two directories:

- `zip` - contains zip files with the executable and necessary auxiliary files
- `win_msi/x64` - contains an MSI installer for `GraphCombEx` (note: both `.msi` and `.cab` are required for successful installation)
