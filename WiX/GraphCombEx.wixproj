<Project Sdk="WixToolset.Sdk/5.0.1">
  <PropertyGroup>
    <!-- Ensure the platform is set to x64 if not explicitly specified -->
    <Platform Condition=" '$(Platform)' == '' ">x64</Platform>
  </PropertyGroup>
</Project>